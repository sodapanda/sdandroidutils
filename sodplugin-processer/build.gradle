apply plugin: 'java'

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile deps.autocommon
    compile deps.javapoet
    compileOnly deps.autoservice
    compile project(':sodanno')
}
